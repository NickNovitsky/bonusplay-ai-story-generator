'use server'

// This action performs generating of book outline from single idea

// eslint-disable-next-line
export async function createBookOutline(idea: string) {  

  return {
    result: [
        "Line 1",
        "Line 2"
    ]
  }
}